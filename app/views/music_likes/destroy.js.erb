$("#like_form-<%= @music_post_id.id %>").html
  (
    "<%= escape_javascript(render partial:'music_posts/like', locals: { music_post: @music_post_id }) %>"
  );
$("#like-<%= current_user.id %>").html('<%= current_user.music_likes.count %>');
