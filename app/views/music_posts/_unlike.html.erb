<%= form_with model: current_user.music_likes.find_by(music_post_id: music_post.id),
              html: { method: :delete } do |f| %>
  <div><%= hidden_field_tag :music_post_id, music_post.id %></div>
  <%= f.submit "お気に入り解除", class: "btn btn-secondary" %>
<% end %>
